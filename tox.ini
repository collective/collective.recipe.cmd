# Generated from:
# https://github.com/zopefoundation/meta/tree/master/config/buildout-recipe
[tox]
minversion = 3.18
envlist =
    release-check
    lint
    py310-plone61
    py311-plone61
    py313-plone61
    py310-plone62
    py312-plone62
    py314-plone62
    coverage

[testenv]
usedevelop = true
deps =
    plone61: -c https://dist.plone.org/release/6.1-latest/constraints.txt
    plone62: -c https://dist.plone.org/release/6.2-latest/constraints.txt
    setuptools
commands =
    zope-testrunner --test-path=src {posargs:-vc}
extras =
    test
setenv =
    PYTHONWARNINGS = ignore:pkg_resources is deprecated:UserWarning

[testenv:setuptools-latest]
basepython = python3
deps =
    git+https://github.com/pypa/setuptools.git\#egg=setuptools

[testenv:release-check]
description = ensure that the distribution is ready to release
basepython = python3
skip_install = true
deps =
    setuptools >= 78.1.1,< 81
    wheel
    twine
    build
    check-manifest
    check-python-versions >= 0.20.0
    wheel
commands_pre =
commands =
    check-manifest
    check-python-versions --only pyproject.toml,setup.py,tox.ini,.github/workflows/tests.yml
    python -m build --sdist --no-isolation
    twine check dist/*

[testenv:lint]
description = This env runs all linters configured in .pre-commit-config.yaml
basepython = python3
skip_install = true
deps =
    pre-commit
commands_pre =
commands =
    pre-commit run --all-files --show-diff-on-failure

[testenv:coverage]
basepython = python3
allowlist_externals =
    mkdir
setenv =
    {[testenv]setenv}
    COVERAGE_PROCESS_START={toxinidir}/pyproject.toml
    COVERAGE_FILE={toxinidir}/.coverage
deps =
    setuptools >= 78.1.1,< 81
    coverage[toml]
commands =
    mkdir -p {toxinidir}/parts/htmlcov
    coverage erase
    coverage run -m zope.testrunner --test-path=src {posargs:-vc}
    coverage combine
    coverage html
    coverage report
